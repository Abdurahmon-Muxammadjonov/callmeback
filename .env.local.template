# ==============================================================================
# PROSELL CORE BACKEND - ENVIRONMENT VARIABLES TEMPLATE
# ==============================================================================
# Copy this file to '.env.local' in the root directory and fill in actual credentials.
# WARNING: NEVER commit the '.env.local' file or share secret keys in client-side code.

# 1. Supabase Client Configuration (Safe for client-side prefix if needed for auth/anon reads)
NEXT_PUBLIC_SUPABASE_URL=https://cwdpjikwbkbosbairqym.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_ShsFCxZ5OozTyXOXFT8efw_mJlF6DRm
NEXT_PUBLIC_API_URL=http://localhost:5001

# 2. SERVER-ONLY SECRETS (CRITICAL SECURITY RISK: NEVER prefix with NEXT_PUBLIC_!)
# These keys grant full database bypass (RLS bypass) and API credits access.
# They are strictly isolated to server-side code scopes via the 'server-only' package.
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here
GROQ_API_KEY=your_groq_api_key_here
SUPABASE_JWKS_URL=https://cwdpjikwbkbosbairqym.supabase.co/auth/v1/.well-known/jwks.json

# 3. Server Configuration
PORT=5000

# 4. PBX History Sync (Optional tuning for ECONNRESET resilience)
# Retry / timeout
PBX_RETRY_COUNT=3
PBX_RETRY_DELAY_MS=1500
PBX_REQUEST_TIMEOUT_MS=20000
PBX_AUDIO_TIMEOUT_MS=90000

# Chunking and write batching
# chunk unit: day | month
PBX_SYNC_CHUNK_UNIT=day
PBX_SYNC_CHUNK_SIZE=7
PBX_WRITE_BATCH_SIZE=20


